# -----------------------------------------------------------------------------
# Instagram credentials (required)
# -----------------------------------------------------------------------------
# Use either INSTAGRAM_* or test_username / test_password
INSTAGRAM_USERNAME=your_instagram_username
INSTAGRAM_PASSWORD=your_instagram_password

# -----------------------------------------------------------------------------
# Proxy (optional)
# -----------------------------------------------------------------------------
# SOCKS5 proxy for all requests (Instaloader + Selenium). Leave empty to disable.
# Example: socks5h://127.0.0.1:10808  (socks5h = DNS via proxy)
PROXY_URL=socks5h://127.0.0.1:10808

# -----------------------------------------------------------------------------
# Paths (optional)
# -----------------------------------------------------------------------------
# Where to save downloaded media. Default: downloads
# DOWNLOAD_DIR=downloads

# Session file path. Default: DOWNLOAD_DIR/session-<USERNAME>
# Set to a full path if you copy a session from another machine.
# SESSION_FILE=downloads/session-your_username

# -----------------------------------------------------------------------------
# Selenium headless browser (optional)
# -----------------------------------------------------------------------------
# Use headless browser when API login fails (e.g. checkpoint). Default: 1 (on).
# Set to 0 to show the browser (debugging).
# SELENIUM_HEADLESS=1

# -----------------------------------------------------------------------------
# Chromium/Chrome on server (optional, for headless login on VPS)
# -----------------------------------------------------------------------------
# If Chromium is not in PATH, set the binary path (e.g. apt install chromium-browser).
# CHROME_BIN=/usr/bin/chromium
# CHROMIUM_BIN=/usr/bin/chromium-browser

# Path to chromedriver if not using webdriver-manager (e.g. apt install chromium-chromedriver).
# CHROMEDRIVER_PATH=/usr/bin/chromedriver
